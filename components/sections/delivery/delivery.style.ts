import styled from "@emotion/styled";
import { getCurrentGap } from "../../../styles";

export const DeliverySectionWrapper = styled.section`
  display: flex;
  flex-direction: column;
  ${getCurrentGap('pig')}
`;
